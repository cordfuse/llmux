{
  "name": "llmux-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "tmux-based AI agent session manager — dispatch to interactive agent CLIs via tmux send-keys",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "llmuxd": "tsx packages/llmuxd/src/index.ts",
    "llmux": "tsx packages/llmux/src/index.ts",
    "build": "npm run build --workspace @cordfuse/llmuxd && npm run build --workspace @cordfuse/llmux",
    "typecheck": "npm run typecheck --workspace @cordfuse/llmuxd && npm run typecheck --workspace @cordfuse/llmux"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0",
    "tsup": "^8.3.0",
    "tsx": "^4.19.0"
  },
  "engines": {
    "node": ">=20"
  }
}
