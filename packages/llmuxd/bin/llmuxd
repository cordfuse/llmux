#!/usr/bin/env node
import('../dist/index.js').catch(async (err) => {
  // Source fallback for dev (no dist): bun handles ts directly.
  if (err?.code === 'ERR_MODULE_NOT_FOUND') {
    await import('../src/index.ts');
  } else {
    throw err;
  }
});
