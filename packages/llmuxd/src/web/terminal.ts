import { notImplemented } from '../cli.ts';

/**
 * xterm.js WebSocket bridge — bidirectional bytes between a browser xterm
 * instance and a node-pty attached to the tmux session.
 *
 * Phase 4 fills this in.
 */
export function attachWebTerminal(_sessionName: string, _socket: unknown): void {
  notImplemented('web.attachWebTerminal');
}
